apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: products-service
  namespace: argocd
spec:
  destination:
    namespace: webshop
    server: https://kubernetes.default.svc
  source:
    path: kubernetes/products-service
    repoURL: https://github.com/DHBW-Malte/devops-lecture-project.git
    targetRevision: HEAD
  sources: []
  project: default
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
      enabled: true
    syncOptions:
      - CreateNamespace=true

